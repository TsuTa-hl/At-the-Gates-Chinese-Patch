# 当前状态契约

仓库不将带日期的散文视为验证证据。当前状态只能由一次新的
`Invoke-AtGVerification.ps1` 结果及其构建报告、xUnit 输出、事务清单和烟测产物
确立。

- 开发基线：已恢复的当前 Steam 原版。
- 默认范围：构建、静态 xUnit 套件、假游戏发布包往返、事务安装和主菜单烟测。
- 黑盒 UI 覆盖：选择加入，并记录在所属场景文件中。
- 纯静态结果：明确受限；它不是视觉验证。
- 历史本地化安全性：仅保留在 `translations/localization-safety-registry.json`。
- 本文件只记录仓库级重构和验证状态；不记录单项本地化修复、具体黑盒结果或用户
  主导的待人工测试项目。

## 当前重构验证

- DynamicCjk 构建和构建报告契约：通过。
- 源捕获和验证预检重构：完整锁定 xUnit 套件通过。
- 接口进度重构：源 catalog 审计会拒绝部分 catalog 导入，而不接受不完整输入。
- 事务和发布包重构：假游戏刷新/恢复、卸载恢复和自包含发布包检查通过。
- 统一验证（2026-08-07）：针对已恢复 Steam 基线的源捕获、原子 DynamicCjk 构建、
  锁定静态套件、事务安装和 xUnit 所有的主菜单烟测均通过。事务清单验证每个已安装
  补丁哈希，且未写入新的崩溃日志。

只有仓库级重构或验证契约变更才可新增当前状态条目。单项本地化修复和任何人工
测试状态保留在当前任务交接或场景所有者中。
